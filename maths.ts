const add = (x: number, y: number): string => {
    console.log((x+y).toString());
    return (x + y).toString();
}

add(8, 6);

const minus = (a: number, b: number) => {
    console.log(a - b);
}

minus(8, 6);
